#!/usr/bin/env bash

PASSIM_HOME="$(cd "`dirname "$0"`"/..; pwd)"

SPARK_SUBMIT_ARGS="$SPARK_SUBMIT_ARGS"

spark-submit --class PassimApp --packages 'com.github.scopt:scopt_2.10:3.3.0' \
	     $SPARK_SUBMIT_ARGS \
	     "$PASSIM_HOME"/target/scala-2.10/passim_2.10-1.0.0.jar $@
